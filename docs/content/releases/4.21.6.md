**New Features**

* Added the `JwtProvider.forEnvironment` function.

**Bug Fixes**

* Corrected failure of `JwtProvider` to include `expirationInMinutes` parameter when generating impersonation tokens.

**Other**

* Updated example page, adding support for TGAM staging environment.